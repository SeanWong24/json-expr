{
  "name": "json-ex",
  "version": "0.0.0",
  "description": "JsonEx is a JSON-based customizable expression syntax that allows you to define and execute logic securely. By controlling the set of available built-in functions, it provides the flexibility for a wide range of tasks, from simple calculations to more advanced logic, all within a safe execution environment.",
  "main": "dist/mod.js",
  "types": "dist/mod.d.ts",
  "type": "module",
  "files": ["dist/"],
  "scripts": {
    "cli": "npm run build && node dist/cli",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/minimist": "^1.2.5",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "minimist": "^1.2.8"
  }
}
