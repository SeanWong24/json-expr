{
  "name": "@seanwong24/json-x",
  "version": "0.1.1",
  "description": "JSON-X is a JSON-based customizable expression syntax that allows you to define and execute logic securely. By controlling the set of available built-in functions, it provides the flexibility for a wide range of tasks, from simple calculations to more advanced logic, all within a safe execution environment.",
  "keywords": [
    "json",
    "expression"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SeanWong24/json-x.git"
  },
  "main": "dist/mod.js",
  "types": "dist/mod.d.ts",
  "type": "module",
  "files": [
    "dist/"
  ],
  "bin": "dist/cli.js",
  "scripts": {
    "cli": "npm run build && node dist/cli",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/minimist": "^1.2.5",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "minimist": "^1.2.8"
  }
}
